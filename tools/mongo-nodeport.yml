apiVersion: v1
kind: Service
metadata:
  name: mongo-node
  namespace: ruijie-sourceid
spec:
  type: NodePort
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 30031
  selector:
    name: mongo1