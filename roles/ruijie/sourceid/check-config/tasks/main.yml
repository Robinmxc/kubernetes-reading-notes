- name: 检查人脸组件配置参数
  when: 'SOURCEID_DOCKERS.faceid is defined and (SOURCEID_DOCKERS.faceid.enable is not defined or SOURCEID_DOCKERS.faceid.enable)'
  block:
  - name: 检查是否已经设置FDFS参数
    fail: msg="安装人脸组件需要先设置FDFS参数"
    when: 'FDFS_MODE == "none"'

