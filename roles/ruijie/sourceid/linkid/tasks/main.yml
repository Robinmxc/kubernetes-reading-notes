
- name: 复制配置文件
  copy:
    src: "{{ workspace_dir }}/conf/sourceid/linkid/conf"
    dest: "{{ config_base_dir }}/sourceid/linkid/"

- name: 准备目录
  file: path={{ item }} state=directory
  with_items:
  - "{{ yaml_dir }}/linkid"
  run_once: true
  connection: local

- name: 准备部署文件
  template: src=linkid.yaml.j2 dest={{ yaml_dir }}/linkid/linkid.yml
  run_once: true
  connection: local

- name: 获取所有已经创建的POD信息
  command: "{{ bin_dir }}/kubectl get pod -n {{ APP_NAMESPACE }}"
  register: pod_info
  run_once: true
  connection: local

- name: 执行部署
  shell: "{{ bin_dir }}/kubectl apply -f {{ yaml_dir }}/linkid/"
  when: '"rg-linkid" not in pod_info.stdout'
  run_once: true
  connection: local
