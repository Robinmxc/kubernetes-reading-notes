- name: 准备目录
  file: path={{ item }} state=directory
  with_items:
  - "{{ yaml_dir }}/sso"

- name: 准备部署文件
  template: src=sso.yaml.j2 dest={{ yaml_dir }}/sso/sso.yml

- name: 获取所有已经创建的POD信息
  command: "{{ bin_dir }}/kubectl get pod -n {{ SOURCEID_NAMESPACE }}"
  register: pod_info

- name: 执行部署
  shell: "{{ bin_dir }}/kubectl apply -f {{ yaml_dir }}/sso/"
  when: '"rg-sso" not in pod_info.stdout'
