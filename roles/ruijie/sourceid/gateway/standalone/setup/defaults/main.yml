docker_version: "{{ SOURCEID_DOCKERS['gateway'].version }}"

img_id_ver: "{{ PRIVATE_INSECURE_REGISTRY }}/{{ SOURCEID_DOCKERS['gateway'].name }}:{{ docker_version }}"

img_file_name: "gateway-{{ docker_version }}.tar"

GATEWAY_PORT: 80

mongodb_port: 27017

GATEWAY_ROUTER_ID: 143
