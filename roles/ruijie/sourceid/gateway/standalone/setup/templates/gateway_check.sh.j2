#!/bin/bash
set -x

A=`docker ps --filter name=rg-gateway -q | wc -l`
if [ $A -eq 0 ];then
   echo `date`':  nginx is not healthy, try to killall keepalived' >> /var/log/keepalived.log
   killall keepalived
fi
