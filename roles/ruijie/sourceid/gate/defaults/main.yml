
gate_java_opt: "{% if CLUSTER_SCALE == 'normal' %}-Xms1792m -Xmx1792m{% elif CLUSTER_SCALE == 'single' %}-Xms1792m -Xmx1792m{% elif CLUSTER_SCALE == 'small' %}-Xms800m -Xmx800m{% else %}-Xms800m -Xmx800m{% endif %}"

gate_configs: []

gate_templates_path: ""

deploy_conf_defaults:
  small:
    replicas: 1
    resources:
      limits:
        cpu: 0.5
        memory: 1Gi
      requests:
        cpu: 0.5
        memory: 512Mi
  normal:
    replicas: 2
    resources:
      limits:
        cpu: 3
        memory: 2560Mi
      requests:
        cpu: 1
        memory: 512Mi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 2
        memory: 2560Mi
      requests:
        cpu: 1
        memory: 512Mi
  basic:
    replicas: 2
    resources:
      limits:
        cpu: 512m
        memory: 1Gi
      requests:
        cpu: 0.5
        memory: 512Mi
  single-basic:
    replicas: 1
    resources:
      limits:
        cpu: 0.5
        memory: 1Gi
      requests:
        cpu: 0.5
        memory: 512Mi
gate_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
CUSTOMIZE_SUFFIX: "cas"