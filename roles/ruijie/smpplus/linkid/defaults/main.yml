linkid_java_opt: "-Xms512m -Xmx1536m"

linkid_configs: []

linkid_templates_path: ""

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 3
        memory: 2Gi
      requests:
        cpu: 0.5
        memory: 1Gi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 3
        memory: 2Gi
      requests:
        cpu: 0.5
        memory: 1Gi

linkid_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
