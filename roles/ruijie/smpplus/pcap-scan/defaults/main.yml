pcap_scan_java_opt:  "--Xms256m -Xmx3072m"

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 2
        memory: 4Gi
      requests:
        cpu: 1
        memory: 1Gi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 2
        memory: 4Gi
      requests:
        cpu: 1
        memory: 1Gi
        
pcap_scan_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
