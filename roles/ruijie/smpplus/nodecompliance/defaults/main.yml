nodecompliance_java_opt:  "-Xms2048m -Xmx10240m"

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 12
        memory: 16Gi
      requests:
        cpu: 1
        memory: 2Gi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 12
        memory: 16Gi
      requests:
        cpu: 1
        memory: 2Gi
        
nodecompliance_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
