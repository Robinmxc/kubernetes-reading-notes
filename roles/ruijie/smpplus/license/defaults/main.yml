license_java_opt: "-Xms256m -Xmx512m"

license_configs: []

license_templates_path: ""

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 1
        memory: 1Gi
      requests:
        cpu: 1
        memory: 512Mi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 1
        memory: 1Gi
      requests:
        cpu: 1
        memory: 512Mi

license_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
