deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 2
        memory: 1Gi
      requests:
        cpu: 2
        memory: 512Mi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 2
        memory: 1Gi
      requests:
        cpu: 2
        memory: 512Mi

socket5_proxy_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
