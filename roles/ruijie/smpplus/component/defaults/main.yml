component_java_opt: "-Xms256m -Xmx512m"

component_configs: []

component_templates_path: ""

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 0.5
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 512Mi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 0.5
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 512Mi

component_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
