nodescanner_java_opt:  "-Xms2048m -Xmx8192m"

deploy_conf_defaults:
  small:
    replicas: 1
  normal:
    replicas: 1
    resources:
      limits:
        cpu: 8
        memory: 12Gi
      requests:
        cpu: 1
        memory: 1Gi
  single:
    replicas: 1
    resources:
      limits:
        cpu: 8
        memory: 12Gi
      requests:
        cpu: 1
        memory: 1Gi
        
nodescanner_deploy_conf: '{{ deploy_conf_defaults[CLUSTER_SCALE] }}'
