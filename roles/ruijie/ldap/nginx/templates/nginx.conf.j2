worker_processes  1;
events {
    worker_connections  1024;
}

# 配置tcp
stream {
    upstream slapd {
        server {{ ldap_1_hostname }}:{{ port }};
        server {{ ldap_2_hostname }}:{{ port }};
    }
    server {
        listen 389;
        proxy_pass slapd;
    }
}
