dn: olcDatabase={2}hdb,cn=config
changetype: modify
replace: olcAccess
olcAccess: {0}to attrs=userPassword,shadowLastChange by dn="cn=admin,{{ LDAP.LDAP_BASE_DN }}" read by auth by self write by none
olcAccess: {1}to dn.base="" by read
olcAccess: {2}to by self read by dn="cn=admin,{{ LDAP.LDAP_BASE_DN }}" write by dn="cn={{ LDAP_READONLY_USER_USERNAME }},{{ LDAP.LDAP_BASE_DN }}" read by * none
