#!/bin/bash
set -x

A=`ps -C traefik --no-header |wc -l`
if [ $A -eq 0 ];then

   echo `date`':  traefik is not healthy, try to killall keepalived' >> /var/log/keepalived.log
   killall keepalived
   
fi
