- name: 获取操作系统版本
  command: cat /etc/system-release
  register: os_info

- name: 检查操作系统版本
  vars:
    os_ver: '{{ os_info.stdout in os_required}}'
  fail: msg="操作系统不属于支持的版本{{os_required}}"
  when: 'os_ver'
