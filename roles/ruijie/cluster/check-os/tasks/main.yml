- name: 获取操作系统版本
  command: cat /etc/system-release
  register: os_info

- name: 检查操作系统版本
  vars:
    os_ver: '{{ os_info.stdout | regex_replace("CentOS.*(\d+\.\d+\.\d+).*", "\1") }}'
  fail: msg="操作系统必须是CentOS 7.6.1810及以上版本"
  when: 'os_ver < os_required'
