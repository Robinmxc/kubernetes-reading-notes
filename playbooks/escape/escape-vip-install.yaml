- hosts:
    - localhost
  tasks:
    - name: 环境检测
      fail:
        msg: "escape-vip-install只支持通过escape部署"
      when: "APP_NAMESPACE != 'ruijie-escape'"

- hosts:
    - escape_node
  vars_files:
    - "/etc/kad/config.yml"
  roles:
    - { role: ruijie/escape/keepalived }
- import_playbook: vip-component-reconfig-escape.yaml
