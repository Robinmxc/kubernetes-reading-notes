- hosts:
  - kube_master
  - kube_node
  gather_facts: true
  vars_files:
  - "{{ KAD_PACKAGE_DIR }}/kad.yml"
  any_errors_fatal: true
  roles:
  - ruijie/escape/check-cpu
  - ruijie/escape/check-mem
  - ruijie/escape/check-disk
