# WARNING: Upgrade the k8s cluster can be risky. Make sure you know what you are doing.
# Read the guide: 'op/upgrade.md' .

# update kubectl binary
- hosts:
  - kube_master
  - kube_node
  - deploy
  roles:
  - prepare

# update masters
- hosts: 
  - kube_master
  roles:
  - kube_master
  - kube_node

# update nodes
- hosts: 
  - kube_node
  roles:
  - { role: kube_node, when: "DEPLOY_MODE != 'allinone'" }
