- hosts:
  - localhost
  tags:
  - env
  roles:
  - ruijie/sourceid/env

- hosts:
  - localhost
  tags:
  - download
  vars_files:
  - "{{ workspace_dir }}/kad.yml"
  roles:
  - ruijie/sourceid/download

- hosts:
  - kube-node
  vars_files:
  - "{{ workspace_dir }}/kad.yml"
  roles:
  - { role: ruijie/sourceid/load-images, tags: ["docker"] }
