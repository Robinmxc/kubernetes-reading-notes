- hosts:
  - localhost
  roles:
  - ruijie/sourceid/env

- hosts:
  - localhost
  tags:
  - download
  vars_files:
  - "{{ setup_file_dir }}/kad.yml"
  roles:
  - ruijie/sourceid/download

- hosts:
  - kube-node
  vars_files:
  - "{{ setup_file_dir }}/kad.yml"
  roles:
  - { role: ruijie/sourceid/load-images, tags: ["docker"] }
  - { role: ruijie/sourceid/prepare, tags: ["sourceid"] }
